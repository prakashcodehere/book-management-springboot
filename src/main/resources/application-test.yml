spring:
  datasource:
    url: jdbc:mysql://localhost:3307/bookdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: bookuser
    password: bookpass

  jpa:
    hibernate:
      ddl-auto: update

  cache:
    type: redis

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 60000

server:
  port: 9090